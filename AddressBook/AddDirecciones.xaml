<Window x:Class="AddressBook.AddDirecciones"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AddressBook"
        Title="Añadir dirección"
        Width="350"
        Height="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Loaded="Window_Loaded">
    <Grid>

        <local:Direcciones HorizontalAlignment="Stretch" VerticalAlignment="Top" x:Name="NewDirection" />
        <Button x:Name="BtnGuardar"
                Width="75"
                Margin="242,434,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Content="Guardar" Click="BtnGuardar_Click" />
        <Button x:Name="BtnCancelar"
                Width="75"
                Margin="162,434,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Content="Cancelar" Click="BtnCancelar_Click" />

    </Grid>
</Window>
